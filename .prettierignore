# Generated/runtime files (ignored by .gitignore but explicit here)
config/
!config/configuration.yaml
custom_components/
!custom_components/pentair_pool/

# Ignore-style files (no recognized extension — Prettier would misparse them)
*ignore

# Machine-generated by release-please; its fixed format (`*` bullets, extra
# blank lines) conflicts with our Prettier config and would break Lint on
# every release. release-please owns this file, so don't lint it.
CHANGELOG.md
