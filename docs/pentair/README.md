# Pentair-specific docs

Project-internal notes that are specific to this integration (as opposed to the HACS blueprint's general guidance under `docs/development/` and `docs/user/`).

- **[STATUS.md](STATUS.md)** — current state, what works, what's pending, where to pick up. **Start here.**
- **[CHANGELOG.md](CHANGELOG.md)** — code-level change log by debugging session.

The full reverse-engineering write-up (Cognito flow, REST endpoints, WS protocol, field-code reference, captured PUT bodies) lives in a separate research workspace and is not required to build or run this integration.
